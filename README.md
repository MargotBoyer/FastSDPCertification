# FastSDPCertification
 A library for formally certifying adversarial robustness of ReLU neural networks via SDP relaxations, whose main contribution (SDPU) certifies all target classes in a single SDP   solve instead of one per class.
